# 基于 ECG 的心律失常超前预警系统 — 项目设计方案

## 一、项目概述

本项目构建了一个基于单导联心电图信号的深度学习心律失常超前预警系统。系统接收过去 10 分钟的心电信号，通过多模态特征融合与因果时序建模，预测未来 5 分钟内可能发生的心律失常事件，并以交互式仪表盘展示诊断结果与病灶溯源热力图。

**核心创新点**：超前预测范式——训练标签取自输入窗口结束后的未来 5 分钟，使模型学习从当前波形中识别未来事件的早期前兆特征，而非被动检测已发生的心律失常。

---

## 二、系统总体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        心电监护预警系统                               │
├─────────────┬─────────────┬─────────────┬─────────────┬─────────────┤
│   数据层    │   模型层    │   推理层    │   展示层    │   评估层    │
│             │             │             │             │             │
│ MIT-BIH    │ WindowEncoder│ FastAPI     │ Streamlit   │ 混淆矩阵    │
│ 原始数据    │ (PTB-XL预训练)│ REST API    │ 动态推演    │ Per-class   │
│             │             │             │             │ P/R/F1/AUC  │
│ 数据预处理   │ Global Skip │ JSON 响应   │ 实时波形    │             │
│ 标签偏移    │ TCN         │             │ 概率趋势    │ 1D-CAM      │
│ 窗口切分    │             │             │             │ 可视化      │
│             │ RR分支      │             │ 1D-CAM      │             │
│ 数据增强    │ 能量包络    │             │ 事件日志    │ 提前量       │
│ 过采样      │ 注意力      │             │             │ 分布分析    │
│             │ 6分类头     │             │             │             │
└─────────────┴─────────────┴─────────────┴─────────────┴─────────────┘
```

---

## 三、模块一：数据层

### 3.1 数据来源

采用 MIT-BIH 心律失常数据库作为核心数据源。该数据库包含 48 条双导联动态心电图记录，每条约 30 分钟，采样率 360Hz，带有逐搏心电专家标注（包括节律类型、心搏类型和辅助注释）。

### 3.2 超前预测数据构建

**输入**：过去 10 分钟 ECG（600 秒 × 250Hz = 150,000 采样点）

**标签**：输入结束后的未来 5 分钟内出现的最严重心律失常类别

```
[──── 过去 10 分钟（模型输入）────|──── 未来 5 分钟（标签取自这里）────]
     39 个重叠窗口                          标注窗口
  (每窗口 30 秒, 步长 15 秒)
```

**六级标签体系（按临床危重度排序）**：

| 标签 | 类别 | 临床含义 |
|------|------|---------|
| 0 | 正常窦性心律 (NSR) | 正常节律，P-QRS-T 完整 |
| 1 | 室性早搏 (PVC) | 宽 QRS 异位搏动，孤立出现 |
| 2 | 心房颤动 (AFib) | P 波消失，RR 间期绝对不齐 |
| 3 | 心室颤动 (VF) | 无序颤动波，无可辨认 QRS |
| 4 | 室性心动过速 (VT) | ≥3 个连续宽 QRS，心率 >100 |
| 5 | 房速/室上速 (AT/SVT) | 窄 QRS 快速规则心律 |

### 3.3 数据预处理

- 四阶巴特沃斯带通滤波（0.5–45Hz），消除基线漂移和高频肌电干扰
- 重采样至 250Hz，统一时间基准
- 滑动窗口切分：39 个重叠窗口（窗口 30 秒 / 7500 点，步长 15 秒 / 3750 点）
- 每个窗口 Z-score 标准化
- 过采样：对少数类（AFib 4×、VF 8×、VT/AT-SVT 4×、PVC 2×）增加样本权重

### 3.4 数据增强

- 基线漂移模拟（0.1–0.5Hz 正弦波叠加）
- 高斯噪声注入（σ=0.02）
- 增益缩放（0.9–1.1 倍）
- 随机组合（每次增强随机选择以上一种）

### 3.5 数据集划分

- 训练集：~39 条 MIT-BIH 记录（排除演示病例和验证专用记录）
- 验证集：4 条 MIT-BIH 记录（102, 104, 107, 217）
- 演示集：5 条记录（100, 119, 209, 201, 207），完全隔离，绝不参与训练

---

## 四、模块二：模型层（ArrhythmiaWarningNet）

### 4.1 总体架构

模型包含四个核心模块，采用多模态融合策略：

```
输入 ECG [B, 39, 1, 7500]
         │
    ┌────┴──────────────────────┐
    │ 模块 2.1: WindowEncoder   │  4 阶段 CNN，PTB-XL 预训练
    │ 输出: [B×39, 256]         │  ─ 波形形态学特征
    └────────────┬──────────────┘
                 │
    ┌────────────┴──────────────┐
    │ 模块 2.2: Global Skip TCN │  3 层因果卷积 (dil=1,2,4)
    │ 输出: [B, 39, 256]        │  ─ 时序演变特征
    └────────────┬──────────────┘
                 │
    ┌────────────┼──────────────┐
    │            │              │
    ▼            ▼              ▼
 [RR 分支]   [能量包络]    [注意力池化]
    │            │              │
    │    RR 样本熵+庞加莱图    │   AvgPool + 包络编码    │   Sigmoid 加权
    └────────────┼──────────────┘
                 │
                 ▼
    ┌──────────────────────────┐
    │ 模块 2.3: 特征融合        │  RR + 包络 + TCN 上下文
    │ 输出: [B, 256]            │
    └────────────┬──────────────┘
                 │
    ┌────────────┴──────────────┐
    │ 模块 2.4: 分类头           │  Linear(256→128→6)
    │ 输出: 6 类 logits + CAM   │
    └───────────────────────────┘
```

### 4.2 模块 2.1 — WindowEncoder（波形形态编码）

四阶段级联 1D 卷积网络，逐层提取多层次心电形态特征：

| 阶段 | 通道变化 | 卷积核 | 降采样方式 |
|------|---------|--------|-----------|
| Stage 1 | 1→64 | k=15, s=2 | MaxPool |
| Stage 2 | 64→128 | k=7, s=2 | MaxPool |
| Stage 3 | 128→256 | k=5, s=2 | Stride |
| Stage 4 | 256→256 | k=3, s=2 | Stride |

每层后接 BatchNorm + ReLU。最后 AdaptiveAvgPool + AdaptiveMaxPool 双池化融合，经 Linear 投影至 256 维。

**PTB-XL 预训练**：WindowEncoder 先在大规模 12 导联心电数据集 PTB-XL（约 21,800 条记录）上进行多标签分类预训练，学习通用心电形态学特征（QRS 宽度、ST-T 形态、传导异常模式），再将预训练权重迁移至本任务。

### 4.3 模块 2.2 — Global Skip TCN（因果时序建模）

3 层膨胀因果时序卷积网络，确保信号处理不泄露未来信息。

| 层 | 通道数 | 膨胀率 | 感受野（窗口步） |
|----|--------|--------|-----------------|
| 1 | 256→256 | d=1 | 3 |
| 2 | 256→256 | d=2 | 7 |
| 3 | 256→256 | d=4 | 15 |

**Global Skip Connection**：每层 TCN 同时输出主路径（残差融合）和跳跃路径（纯净特征），三层跳跃输出经通道适配器汇总后累加。保留多时间尺度特征：浅层捕捉局部波形变化（≈30 秒），中层捕捉中期演变（≈1 分钟），深层捕捉长程趋势（≈2 分钟）。

### 4.4 模块 2.3 — 多模态特征融合

**单模态分支 1（ECG）→ 波形形态与节律分类**：WindowEncoder + TCN 主路径，输出 256 维时序上下文表示，直接驱动 6 类心律失常分类。

**单模态分支 2（ECG）→ 能量包络节律感知**：对原始 ECG 绝对值做 AvgPool1d(k=25, s=25) 包络检波，模拟希尔伯特变换提取振幅包络。Linear(300→64→256) 编码后注入融合层，辅助感知心动过速时的能量脉冲频率变化和 VF 时的振幅混乱模式。

**多模态融合 1（ECG + RR 间期）→ 区分 AFib 与 AT/SVT**：从 ECG 显式提取 R 峰，计算 RR 序列的 9 维特征：

| 特征 | 维度 | 临床意义 |
|------|------|---------|
| mean_rr, sdnn, rmssd, pNN50 | 4 | 逐窗 RR 统计量 |
| 样本熵 | 1 | 量化 RR 不规整度，AFib 显著偏高 |
| 庞加莱图 SD1, SD2, SD1/SD2 比 | 3 | AFib 扇形分布 (SD1≈SD2)，AT 椭圆形 (SD1<<SD2) |
| RR 变异系数 (CV_rr) | 1 | 整体心率波动程度 |

Linear(9→64→256) 编码后注入融合层。医学依据：AFib 的 RR 间期绝对不齐是区别于 AT/SVT（规则快速）的金标准判据。

**多模态融合 2（ECG + 注意力）→ 1D-CAM 可解释热力图**：Sigmoid 时序注意力 Conv1d(256→32→1) 在 39 个窗口上自动学习关键时间段，产生加权池化表示的同时输出 1D-CAM 热力图。热力图直观展示模型在做判断时主要关注了 10 分钟内的哪几个时间段。

### 4.5 模块 2.4 — 分类头

```
Dropout(0.3) → Linear(256→128) → ReLU → Dropout(0.2) → Linear(128→6)
```

输出 6 个 logit 值，经 Softmax 归一化为概率分布。为防止过拟合，分类头采用双 Dropout 结构和适度的隐藏层压缩比（2:1）。

### 4.6 训练策略

| 策略 | 配置 |
|------|------|
| 损失函数 | Focal Loss (γ=2.0) + Label Smoothing (ε=0.12) |
| 类别权重 | NSR:1.0, PVC:2.0, AFib:3.0, VF:8.0, VT:5.0, AT/SVT:4.0 |
| 优化器 | AdamW (lr=3e-4, weight_decay=5e-4) |
| 学习率策略 | Cosine Annealing + Warmup (1500 步) |
| 权重平均 | EMA (衰减率 0.999) |
| 梯度控制 | Gradient Clipping (max_norm=1.0), AMP 混合精度 |
| 早期停止 | Patience=8, 监控指标=Clinical Score |
| 临床评估分数 | 0.5 × Accuracy + 0.5 × Macro F1 |

**Focal Loss** 动态降低高置信度样本的损失权重，使模型聚焦于难分样本（如 AFib vs AT/SVT 边界样本）。**Label Smoothing** 将目标概率从 [1,0,0,0,0,0] 平滑为 [0.88, 0.024×5]，防止过拟合，使决策边界更平滑。**VF 权重 8.0** — 最高临床优先级，漏报代价不可接受。**VT 权重 5.0** — 高危节律，可能数分钟内进展为 VF。

---

## 五、模块三：推理服务层

### 5.1 服务架构

基于 FastAPI 构建 REST API 推理服务：
- GET `/api/health` — 健康检查
- POST `/api/predict` — 接收 180,000 点 ECG 缓冲区，返回预测结果

### 5.2 推理流程

```
客户端发送 ECG 数组 (180,000 点)
    │
    ▼
补齐/截断至标准长度 → Z-score 全局标准化
    │
    ▼
切分为 39 个重叠窗口 (各 7500 点, 步长 3750)
    │
    ▼
逐窗口 RR 特征提取 (R峰检测 → 4维 + 5维轨迹特征)
    │
    ▼
组装为 [1, 39, 1, 7500] 张量 → GPU 推理
    │
    ▼
返回 JSON: {pred_class, class_name, probabilities[6], cam[39]}
```

### 5.3 输出格式

```json
{
  "pred_class": 2,
  "class_name": "心房颤动 (AFib)",
  "probabilities": [0.05, 0.03, 0.78, 0.01, 0.02, 0.11],
  "cam": [0.12, 0.15, ..., 0.89, 0.92]
}
```

---

## 六、模块四：展示层（Streamlit 仪表盘）

### 6.1 界面布局

```
┌──────────────────────────────────────────────┐
│  侧边栏                │     主面板           │
│                        │                      │
│  ● 案例选择            │  诊断卡片            │
│    - 正常窦性           │  ┌──────────────┐    │
│    - 频发PVC           │  │ 心房颤动风险  │    │
│    - AT/SVT           │  │ 置信度 78.3%  │    │
│    - 阵发性AFib        │  └──────────────┘    │
│    - VT/VFl           │                      │
│                        │  ● 实时心电波形      │
│  ● 推演起始点          │                      │
│                        │  ● 心律失常概率      │
│  ● 启动/停止           │    演变趋势          │
│                        │                      │
│  ● 清空日志            │  ● 1D-CAM 热力图     │
│                        │                      │
│                        │  ● 事件日志          │
└──────────────────────────────────────────────┘
```

### 6.2 核心功能

- **动态波形推流**：模拟心电监护仪的扫描覆盖模式，环形缓冲区渲染流畅波形
- **概率趋势图**：60 帧堆叠面积图，展示六类概率的动态演变，直观看到模型判断的切换过程
- **1D-CAM 热力图**：红色区域标示模型做判断时关注的关键时间段，提供可解释性
- **事件日志**：自动记录异常节律事件（去重），颜色编码显示严重程度
- **五类预设案例**：正常窦性 / PVC / AT-SVT / AFib / VT-VFl，覆盖主要临床场景

---

## 七、模块五：评估体系

### 7.1 评估指标

- **混淆矩阵** — 6×6 直观展示各类误判模式
- **Per-class P/R/F1** — 每类的精确率、召回率和 F1
- **OvR Macro AUC** — 一对多 ROC 曲线下面积
- **VT/VF 召回率** — 高危类别漏报率（临床最关心）
- **Per-class AUPRC** — 精确率-召回率曲线下面积（适用于类不平衡）

### 7.2 可解释性评估

- 1D-CAM 热力图可视化：验证模型关注区域是否与实际病灶位置吻合

---

## 八、临床诊断依据对照

| 预测类别 | 关键医学判据 | 模型对应分支 | 临床标准 |
|----------|-------------|-------------|---------|
| NSR | RR 规则 + P-QRS-T 正常 | WindowEncoder + TCN | 心率 60–100，每个 QRS 前有 P 波 |
| PVC | 宽 QRS 形态 + 孤立出现 | WindowEncoder + TCN | QRS >120ms，无关联 P 波 |
| AFib | RR 绝对不齐 + 样本熵高 | RR 分支（主导） | P 波消失，RR 绝对不齐 |
| VF | 波形完全无序 | WindowEncoder + 能量包络 | 无可辨认 QRS-T，频率 150–500 |
| VT | 宽 QRS + 连续出现 | WindowEncoder + TCN | ≥3 个宽 QRS，心率 >100 |
| AT/SVT | 窄 QRS + RR 规则快速 | RR 分支 + TCN | QRS<120ms，HR150–250，规则 |

---

## 九、关键技术决策

### 超前预测 vs 同步检测

传统 ECG 分类模型是"看到什么判什么"——检测已发生的心律失常。本项目的超前预测范式通过标签偏移（标签取自输入窗口后的未来 5 分钟），迫使模型从当前波形中学习未来事件的早期前兆特征。这更贴近临床"预警"而非"报警"的定位。

### 因果卷积 vs 标准卷积

所有时序卷积采用左向填充（Chomp1d），确保当前时刻的表示不依赖未来信息。这是超前预测的必要前提——如果模型能"偷看"未来，预测就没有意义。

### 多尺度跳跃连接

三层 TCN 的跳跃输出独立汇总，同时保留浅层局部变化（PVC 突发）和深层长程趋势（AFib 渐进式 RR 紊乱），比仅用最后一层输出的传统 TCN 信息更丰富。

### 可解释性（1D-CAM）

相比传统黑盒深度学习模型，1D-CAM 热力图让临床人员可以直观看到模型关注的心电信号时间段，增加了系统在临床应用场景中的可信度。
